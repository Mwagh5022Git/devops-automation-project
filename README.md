# My First Project

## Getting started
I created a shell-based automation script that configures AWS CLI dynamically, verifies identity using STS, triggers Terraform provisioning, and post-deployment validates all provisioned services using AWS CLI queries.

Upload these project ( "terraform" Folder and ".gitlab-ci" ) to git lab  
Login to

    https://gitlab.com/

Create New Project

    New Project --> Import project --> GitHub (Import repositories from GitHub) --> Select Repo --> Import
    

